// invert the colors (essentially subtract every color value from the max color
// value which is 255),
//
// * Grayscale the colors, multiply each color value by a constant,
// just make sure your values don't go over 255.
//
// * (red|green|blue)scale the colors, same as above but only multiply one of the colors.


// for(var i = 0; i < 999999; i++){
//   data.writeUIntLE(00255, i, 3);
// }
